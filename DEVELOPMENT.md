# 当前开发版（rust 分支）

## 启动

```sh
./dev.sh
```

CMake Debug 构建后直接运行 Qt 程序，没有 Web 开发服务器或热更新。
本机运行位置：tmux 会话 `opencode` → 窗口 `ECR` → pane `2`。

开发配置隔离在 `build/dev-data/`，不会读取或覆盖原版配置和系统 QSettings。
可用 `ECR_DATA_DIR` 指定其他目录；脚本启动前会备份目录内已有的配置和设置。
正常非 dev 启动仍沿用之前的配置位置。

## 界面

- 保留原版近黑配色、灰色标签、暗蓝运行按钮和经典勾选框/关闭资源，不使用高饱和蓝绿按钮。
- 窗口和托盘统一使用 `resources/app_icon.png`，由原始 logo 生成透明圆角版本，原始 JPG/ICO 不覆盖。
- 前后标签改为页面两侧 SVG 箭头；新建、复制、保存、目录选择和运行采用轻量 SVG 图标。
- 参数表单整体滚动；命令预览和运行栏固定在下方，配置操作独立分组。
- 增量解析从备注框移到专用输入栏，备注不参与执行；参数行可全选、禁用、删除。
- 文本 Ctrl+A 不再被抢占；全部参数使用 Ctrl+Shift+A / Ctrl+D。
- 设置保留菜单/托盘入口并增加底栏按钮；浅色主题保留。
- 与旧版有意不同：不再显示“使用新窗口运行”，运行统一进入内部日志面板。
- Qt5/Qt6、字体、DPI、系统窗口装饰会影响像素差异，不承诺跨系统完全一致。

## 运行与日志

- 点击运行后，日志默认停靠在主窗口下方，也可从“视图 → 运行日志”打开。
- 点击“分离窗口”浮动，点击“停靠”回到主窗口；隐藏/分离不重启或停止任务。
- 每次运行一个新的 `QProcess` + 非交互 shell；各次运行不会共用 shell 状态。
- macOS/Linux 使用 `$SHELL`，未设置时取用户登录 shell，再回退到 `/bin/sh`；以 `-c` 执行。
- Windows 使用 `%COMSPEC%`（通常 cmd.exe），不是启动 Windows Terminal。暂未提供 shell 选择 UI。
- 实时收集 stdout/stderr，记录命令、运行目录、shell、开始时间和退出状态。
- 历史为**当前应用会话**，最多保留 100 次；单次最多 10000 行 / 2 Mi 字符，超出会裁掉最早内容。
- 可切换历史、复制和保存日志；不自动把命令和输出落盘，避免意外保存敏感参数。
- 输出当前按 UTF-8 增量解码。Windows 旧工具的 OEM/ANSI 编码适配仍待完善。
- 停止只针对选中的任务：Unix 使用独立进程组并在宽限期后强制结束；Windows 用 `taskkill /T`。
- 关闭主窗口仍是隐藏到托盘；真正退出时提示仍在运行的任务，并清理它们。
- 这是日志视图，不是 PTY/交互终端；不支持密码提示、REPL、全屏终端程序。
- UI 执行路径使用 Qt 异步进程信号，不调用原 Rust 同步执行 FFI；Rust 继续处理配置和备份。
- 命令分词和重建集中在 `src/core/parser.rs`、`src/core/command_line.rs`：POSIX 使用单引号安全引用，Windows 使用双引号和反斜杠规则；C++ 通过 `rust_build_command` 传结构化参数，不再自行拼接引号。
- 解析时会识别空格、制表符、单双引号、POSIX 反斜杠和 Windows 脱字符；重建时会保护空格、`$`、引号、反斜杠和常见 shell 元字符。
- 参数行中的功能名/参数值按单独参数引用；“其他参数”没有管道/重定向时也会分词，带 `|`、`>`、`&&`、变量等 shell 语法时保留为原始 shell 片段。
- 直接把完整命令粘到“程序”框也能运行：有 shell 语法时按原文执行，否则按跨平台规则重新引用；但建议仍点击“解析”以便编辑参数。
- Windows 的 `%VAR%`、PowerShell 语法和 cmd 的特殊变量展开仍属于 shell 语义；需要字面量时应在命令预览中确认转义结果。

## 验证

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DECR_BUILD_TESTS=ON
cmake --build build --parallel 4
ctest --test-dir build --output-on-failure
```

Qt 测试使用 offscreen 和临时配置，覆盖经典布局/资源、参数预览、标签切换、运行按钮、
UTF-8 分块输出、stderr、退出码、历史切换、分离/停靠、并发隔离、停止和工作目录。
Unix 另测忽略 SIGTERM 的派生子进程清理。Windows 分支需要在 Windows 上实测。
可用 `ECR_UI_ARTIFACT_DIR=/tmp/ecr-ui-artifacts ctest --test-dir build` 导出虚构配置的界面截图/几何数据。

## 本轮修复

- 接通完整/增量解析，并修正 C++/Rust 解析 FFI 参数签名不一致。
- 保留单双引号，支持制表符/换行分词；拒绝用不完整引号覆盖现有参数。
- 修复空功能名但有参数值时被漏掉、负数被当作标志、Unix 绝对路径被当作标志等问题。
- 勾选状态随保存/加载/复制保留；支持 PyQt5 字段格式迁移和早期二元数组参数格式。
- 复制标签自动生成不重名标题；重载前提示保存修改；描述按纯文本恢复。
- 损坏/不可读配置阻止自动覆盖；备份恢复前保留安全副本。
- 自动备份忽略 JSON 排版差异，文件名纳秒精度避免同秒保存覆盖备份。
- 恢复文件拖入、文件剪贴板粘贴及行编辑框的右键路径粘贴，不再硬改路径分隔符。
- 设置取消会恢复预览前的主题；无系统托盘时关闭不会导致窗口永久消失。
- 补齐 `tempfile` 测试依赖，现在 `cargo test` 可运行。

语言翻译资源、Windows 输出编码及跨平台打包仍待完善。
历史日志只保留当前会话；复杂 shell 语法仍建议核对最终预览后再执行。
早期 `COMPLETION_REPORT.md` 中“全部完成”的表述不代表当前功能状态。
